{
  "name": "@greenlight/gl-client-js",
  "version": "0.1.4",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "neon build --release",
    "build-debug": "npm run build",
    "build-release": "npm run build",
    "install": "node-pre-gyp install --fallback-to-build=false || neon build",
    "test": "cargo build && cargo test && jest",
    "package": "npm run build && node-pre-gyp package"
  },
  "binary": {
    "module_name": "index",
    "host": "https://storage.googleapis.com/greenlight-artifacts/npm/gl-client-js/",
    "remote_path": "{version}",
    "package_name": "gl-client-js-napi{napi_version}-{platform}-{arch}.tar.gz",
    "module_path": "."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.3.1",
    "rewire": "^5.0.0"
  },
  "dependencies": {
    "@mapbox/node-pre-gyp": "^1.0.7",
    "neon-cli": "^0.9.1",
    "protobufjs": "^6.11.2"
  }
}

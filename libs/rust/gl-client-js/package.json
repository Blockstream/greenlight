{
  "name": "gl-client-js",
  "version": "0.1.0",
  "description": "",
  "main": "index.node",
  "scripts": {
    "build": "neon build --release && neon-tag-prebuild",
    "build-debug": "npm run build",
    "build-release": "npm run build",
    "install": "npm run build-release",
    "test": "cargo test && jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.3.1",
    "rewire": "^5.0.0"
  },
  "dependencies": {
    "neon-cli": "^0.9.1",
    "neon-load-or-build": "^2.2.2",
    "neon-tag-prebuild": "^1.1.0",
    "protobufjs": "^6.11.2"
  }
}
